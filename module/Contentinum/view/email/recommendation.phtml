<?php 
$html = '<div class="row"><div class="large-12 columns">';
if ($this->success){
    $html .= '<p>Die E-Mail wurde erfogreich versandt.</p>';
} else {
    if ($this->form){
        $html .= $this->renderForm($this->form);        
    }
}
echo '</div></div>';
if (true === $this->xmlHttpRequest){
    $html .= '<a class="close-reveal-modal" aria-label="Close">&#215;</a>';
    echo $html;
    exit();
} else {
    $groupstyles = $this->groupstyles['styles'];
    $htmlwidgets = $this->htmlwidgets['widgets'];
    echo $html;
    $this->layoutassets($this->htmllayouts,$this->templateKey,$this->htmlassets);
    $layout = $this->buildlayout($this->htmllayouts,$this->templateKey);
}
?>